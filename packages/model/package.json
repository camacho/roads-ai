{
  "name": "@roads-ai/model",
  "private": true,
  "version": "0.0.0",
  "description": "Local-only model adapter contract for roads-ai",
  "type": "module",
  "main": "index.ts",
  "types": "types.ts",
  "exports": {
    ".": "./index.ts",
    "./package.json": "./package.json"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run"
  },
  "keywords": [
    "model",
    "local",
    "teaching",
    "harness"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3",
    "vitest": "catalog:"
  }
}
